# Authentication Module

RFID, fingerprint, face recognition, behavior checks, and enrollment helpers.

Helper file paths are intentionally kept stable because the FSM and dashboard call them through subprocesses.
